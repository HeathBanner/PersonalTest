import React, { PureComponent } from 'react';

export default class GameOver extends PureComponent {
    render() {
        return (
            <>
                <h1>Finished!</h1>            
            </>
        );
    };
};